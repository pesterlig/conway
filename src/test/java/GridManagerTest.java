import org.junit.Assert;
import org.junit.Test;


public class GridManagerTest {
    @Test
    public void every_Cell_in_the_cells_array_is_dead(){
        Cell cell = new Cell(false);
        GridManager gridManager = new GridManager(10, 9);
        //make an array and put cells in it
        //make a loop to check each cell and count live ones - if live cells is == 0,
        //then the test should pass









        Assert.assertTrue();

    }


}
